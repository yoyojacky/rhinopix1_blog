# AGENTS.md — rhinoPix1 Blog

> 这是 rhinoPix1 个人博客的工作约定文档。所有 AI 助手 (Claude Code, Copilot, Cursor 等) 在这个仓库工作前，必须先读这份文档。

## 1. 项目身份

- 站点：rhinoPix1 personal blog
- 框架：Docusaurus 3.10.2 (classic preset)
- 语言：中英双语 (default en, zh-Hans)
- 主题：Cyberpunk neon 赛博朋克
- 仓库：git@github.com:yoyojacky/rhinopix1_blog.git
- 部署：GitHub Pages (yoyojacky.github.io/rhinopix1_blog/)

## 2. 端口约定

| 服务           | 端口   | 命令            |
|----------------|--------|-----------------|
| dev server     | 50011  | yarn start      |
| serve build    | 50012  | yarn serve      |

> 不要改端口。Pi 上 deskpi-docs 已占 50009/50010。
> 监听 0.0.0.0，浏览器请用 http://<pi-ip>:50011/rhinopix1_blog/ 访问。

## 3. 目录结构

    rhinoPix1/
    ├── docs/                       # 知识库 (长期沉淀)
    ├── blog/                       # 博客文章 (带日期)
    │   ├── authors.yml             # 博客作者注册表
    │   └── YYYY-MM-DD-<slug>.md
    ├── src/
    │   ├── css/custom.css          # 赛博朋克主题 (博客灵魂)
    │   ├── pages/                  # 自定义单页 (about 等)
    │   └── components/             # 自定义 React 组件
    ├── static/img/                 # 静态资源
    ├── i18n/zh-Hans/               # 中文翻译 (UI + blog posts)
    ├── docusaurus.config.js
    ├── sidebars.js
    └── package.json

## 4. 核心红线

1. 不要碰 deskpi-docs。它和本博客无关。
2. 不要提交 node_modules、build。已在 .gitignore，禁止 git add -f。
3. 不要擅自改端口 50011/50012。
4. 不要删除赛博朋克主题 (src/css/custom.css)。可以扩展，不要重写或删除现有规则。
5. 不要直接修改 node_modules 任何文件。要 patch 先建 patches/ 用 patch-package。
6. 不要手动 git add 构建产物。只提交源码。

## 5. 博客写作规范

### Frontmatter (必填)

    ---
    slug: my-post-slug
    title: My Post Title
    authors: [rhinoPix1]
    tags: [tag1, tag2]
    ---

- 正文语言与 title 一致 (英文 post 写英文，中文 post 写中文)。
- 必须放 <!-- truncate --> 标记，让列表页只显示摘要。
- 图片放 static/img/posts/<slug>/，用相对路径 /img/posts/<slug>/xxx.png。
- 代码块用三反引号 + 语言标识。
- 双语策略：英文版写 blog/，中文版写 i18n/zh-Hans/docusaurus-plugin-content-blog/，两版 slug 必须相同。

## 6. 文档写作规范 (docs/)

- 最高级标题从 ## H2 开始 (# H1 由 Docusaurus 自动从 title 生成)。
- 内部链接用相对路径，如 [About](../about)。
- 新建文档无需手动注册，sidebar 用 autogenerated 模式自动收录。
- 但需要把新文档的链接加入 docs/index.md 或相关索引页。

## 7. 主题定制

所有视觉调整只改 src/css/custom.css。CSS 变量已在文件顶部：

    :root {
      --rhp-bg: #0a0e1a;          /* 深空黑 */
      --rhp-neon-cyan: #00f0ff;   /* 霓虹青 */
      --rhp-neon-pink: #ff2e88;   /* 霓虹粉 */
      --rhp-neon-purple: #b16cff; /* 霓虹紫 */
    }

改颜色时只动变量值，别动规则本身。

## 8. 本地工作流

    yarn start    # 启动 dev server，浏览器自动热更新
    yarn build    # 生成 build/
    yarn serve    # 生产预览 http://localhost:50012/

部署 (GitHub Pages) 后续用 GitHub Actions 自动部署，先在 .github/workflows/ 加 deploy.yml。

## 9. Git 约定

- 不要直接 commit 到 main 分支，新功能/文章用 feature 分支。
- Commit message 格式：
  - blog: <date>-<slug>     新文章
  - docs: <简述>             文档变更
  - style: <简述>            CSS / 主题调整
  - chore: <简述>            配置 / 依赖
  - fix: <简述>              bug 修复
- 每次完成一个原子改动就 commit，不要攒一大堆一起交。

## 10. 故障排查速查

- 启动报 Cannot find lunr.zh-Hans.js
  → 搜索插件不支持 zh-Hans，改 language: ['en', 'zh']
- 启动报 blog.blogSidebarTitle is not allowed
  → Docusaurus 3.x 移除了该选项，删除 themeConfig.blog 块
- 启动报 authors map file could not be loaded
  → frontmatter 引用了未注册的作者，创建 blog/authors.yml 并配置 authorsMapPath
- Windows 访问 http://<pi-ip>:50011/ 无响应
  → Pi 防火墙拦截，sudo ufw allow 50011/tcp
- 改文件不生效
  → dev server 卡住，Ctrl+C 停掉后 yarn start

## 11. rhinoPix1 工作偏好

- 逐步推进：每次只做一个原子步骤，做完确认再做下一步。
- 报错贴日志：碰到报错，贴出错的最后 15-25 行，别只说失败了。
- 少问多做：用合理默认值推进，关键决策才停下来问。
- 中文为主：对话、AGENTS.md、文档说明以中文写，文章内容语言看场景。
- 赛博朋克风：所有视觉调整往霓虹/未来感走，不要温馨极简。
